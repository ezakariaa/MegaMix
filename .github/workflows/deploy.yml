name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: client/package.json

      - name: Install dependencies
        run: |
          cd client
          # Utiliser npm ci si package-lock.json existe, sinon npm install
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Build
        run: |
          cd client
          echo "=========================================="
          echo "🔍 Vérification de la configuration API"
          echo "=========================================="
          if [ -z "$VITE_API_URL" ] || [ "$VITE_API_URL" = "http://localhost:5000" ]; then
            echo ""
            echo "❌ ERREUR CRITIQUE: VITE_API_URL n'est pas configuré!"
            echo "❌ L'application ne pourra PAS se connecter au backend sur GitHub Pages."
            echo ""
            echo "📋 SOLUTION:"
            echo "   1. Allez dans GitHub → Settings → Secrets and variables → Actions"
            echo "   2. Créez un secret nommé: VITE_API_URL"
            echo "   3. Valeur: https://muzak-server-production.up.railway.app"
            echo "   4. Redéployez l'application après avoir ajouté le secret"
            echo ""
            echo "⚠️  Le build va continuer mais l'application ne fonctionnera pas sur GitHub Pages!"
            echo ""
          else
            echo "✅ VITE_API_URL configuré: $VITE_API_URL"
            echo "✅ L'application devrait pouvoir se connecter au backend"
          fi
          echo "=========================================="
          echo ""
          npm run build
        env:
          # Utilisez votre URL de backend déployé ici
          # Configurez VITE_API_URL dans Settings > Secrets and variables > Actions
          # Exemple: https://muzak-server-production.up.railway.app (sans /api à la fin)
          VITE_API_URL: ${{ secrets.VITE_API_URL || 'http://localhost:5000' }}

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'client/dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4


